import { tablePropertyInput } from '../../../helpers/table-property-input';
import { module, test } from 'qunit';

module('Unit | Helper | table property input');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = tablePropertyInput(42);
  assert.ok(result);
});
