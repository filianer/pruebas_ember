import { tablePropertyValue } from '../../../helpers/table-property-value';
import { module, test } from 'qunit';

module('Unit | Helper | table property value');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = tablePropertyValue(42);
  assert.ok(result);
});
